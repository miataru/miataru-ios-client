import SwiftUI

struct iPhone_OnboardingServerView: View {
    var body: some View {
        VStack {
            Text("Server URL")
            Text("Please enter your server URL ...")
        }
    }
}

#Preview {
    iPhone_OnboardingServerView()
} 
